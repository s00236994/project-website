- install libraries required
npm install
- run the server
node app.js


Leeds United Website  
Martin Durkin   

Changes Made:

 Replaced custom navigation with Bootstrap navbar (responsive and active links highlighted).  
 Applied Bootstrap container, row, and col classes to all pages for proper grid layout.  
 Updated contact form to use Bootstrap classes: form-label, form-control, form-check, btn btn-primary.  
 Tables updated with Bootstrap classes: table, table-striped, table-bordered, table-responsive.  
 Replaced inline styling with Bootstrap utilities for headings, text, images, and footer; pages are now fully responsive.
